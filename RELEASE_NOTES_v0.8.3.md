# PDF 单页合并器 v0.8.3 正式版

> 📦 适用于 **Windows 10/11 64 位**。解压后双击 `build.bat` 一键打包出 **PDFMerger_v0.8.3.exe**（单文件、无黑命令行窗口、带图标、带版本信息）。

---

## ✨ 一句话介绍

把多份**单页 PDF** 按 2 / 4 / 6 / 8 合 1 排版成一张 A4 / A3 纸，打印或共享一目了然。文件数超过模式容量时**自动分页**，导出支持 PDF / JPG / PNG，带合并预览、运行日志、配置持久化。

---

## 🆕 v0.8.3 更新内容（最新）

### 🎯 三栏高度比例调整为 57% / 27% / 16%
- 合并预览占 57%（扩大预览视野）、输入文件 / 合并设置 / 页间距 占 27%、运行日志 + 导出 占 16%
- 修复 tkinter 3.14+ 分隔条定位 API（`sash_place` / `sashpos`）不一致导致"一直是 1:1:1 均分"的问题

---

## 🆕 v0.8.2 更新内容（重要功能里程碑）

### 🖨️ 文件数超出模式容量 → 自动分页
- **修复关键 bug**：3 个文件 + 2 合 1 模式，之前只输出 1 页（第 2 张被截断）
- **现在**：第 1 页排文件 1 / 2，第 2 页按相同 2 合 1 缩小格式排文件 3（占左槽位）
- 通用公式：`页数 = ceil(文件数 / 模式容量)`。例：9 个文件 4 合 1 → 3 页；7 个文件 6 合 1 → 2 页
- **PDF 导出**：单文件多页 PDF；**JPG / PNG 导出**：自动生成 `xxx_p1.jpg`、`xxx_p2.jpg`… 多张图，兼容原单页用原名

### 👁️ 预览多页垂直拼接 + 页码元信息
- 多页时预览图纵向拼接所有输出页（页间淡灰色缝），一张图看完整结果
- 顶部元信息栏显示：`4合1 · A4 横向 · 2×2 · 间距 10/10mm · 9 个文件 · 共 3 页`
- 预览清晰度升级为 `target_w = 1600px`（原 800+），放大/缩放下文字不糊

### ✒️ 字体彻底统一黑体加粗（解决发虚 / 英文字体残留）
- 界面所有控件 **全部使用 `SimHei bold`（黑体加粗）**
- 双保险兜底：tk 命名字体 + customtkinter 主题默认字体（family / weight）双双覆盖，杜绝 SegmentedButton 等内部元素回退 Roboto 细体

### 📜 文件列表滚动条智能显隐
- 文件 ≤ 5 个时右侧黑色滚动条自动隐藏，界面干净
- ≥ 6 个自动恢复滚动，不影响功能

### 🎛️ 三栏布局 55% / 25% / 20%
- 上一轮 v0.8.1 的 60/20/20 改为 55/25/20，中部三卡空间更合理（v0.8.3 再微调为 57/27/16）

---

## 🆕 v0.8.1 更新内容（基础架构里程碑）

### 🔢 版本号管理 + exe 名带版本
- `version.py` 与 `version_info.txt` 双源同步，`bump_version.py 0.8.x` 一键升级
- `build.spec` 自动从源码读取版本，exe 名固定为 `PDFMerger_v<版本>.exe`；Windows 右键属性 → 详细信息可见版本号

### 🖱️ 合并预览：手型拖动 + 鼠标滚轮缩放
- 悬停即手型光标（`hand2`），按住左键可拖动画面；滚轮上滚放大 / 下滚缩小（0.25× ~ 6×）；顶部 － / 1:1 / ＋ 三键快速切换

### 📐 三栏拖拽分隔条布局（PanedWindow）
- 预览 60% / 中部三卡 20% / 日志+导出 20%，分隔条悬停双箭头可自行拖曳

### 🗂️ 输入文件卡左右布局
- 左：淡蓝底文件列表；右：上移 / 下移 / 删除 / 清空 / 添加 5 个竖排分色按钮

### 📁 另存为按钮靠右对齐
- 导出卡内操作按钮整体右对齐，版本号标签也在右下角

### 🔤 全中文黑体统一
- 首轮全局字体统一，消除 90% 英文默认字体

### 🪟 默认界面 1240×940
- 最小尺寸 1120×840，适合大显示器也不失真

---

## 📦 发布包说明

| 文件 | 说明 |
|---|---|
| `pdf_merger_v0.8.3.zip` | **源码包 + 构建脚本**（本 Release 附件），解压后直接运行 `build.bat` 即可生成 exe。**下载后不要进子目录再 build，就在解压根目录跑 build.bat**（脚本自动把 exe 输出到解压根目录）|
| 生成的 `PDFMerger_v0.8.3.exe` | Windows 单文件可执行程序（本地 build 后产出），无控制台黑窗、带应用图标、右键属性可见版本 `0.8.3.0` |

> ⚠️ 由于 exe 为 Windows PE 可执行文件，沙箱环境无法直接交叉编译。请在你的 Windows 环境下解压源码 zip → 进入目录 → 双击 `build.bat`，产物 exe 会出现在解压根目录。

---

## 🛠️ 核心功能总览

| 分类 | 能力 |
|---|---|
| **合并模式** | 2 合 1 / 4 合 1 / 6 合 1 / 8 合 1，可选 A4 / A3 × 横 / 纵，文件超出自动分页 |
| **可调参数** | 横向间距（mm）/ 纵向间距（mm）/ 外边距（mm）/ DPI（JPG·PNG 生效，默认 300）|
| **导出格式** | PDF（矢量保真）/ JPG / PNG（按 DPI 光栅化；多页自动生成多张） |
| **文件管理** | 添加 / 删除 / 上移 / 下移 / 清空 + 运行后自动恢复上次文件列表和设置 |
| **预览交互** | Canvas 渲染、手型拖动、鼠标滚轮缩放（0.25×~6×）、1:1 还原、多页拼接 |
| **其他** | 界面字体全黑体加粗、运行日志分等级色标、版本号在标题栏 & 导出区双显 |

---

## 🧪 质量验证

- `pytest` 39 项全部通过（版式 / 多页布局 / 导出示例 / 预览 PNG / 持久化）
- UI 冒烟测试：`UI_SMOKE_OK`
- 预览管线冒烟测试：`PREVIEW_PIPELINE_OK (canvas has image: True)`
- 虚拟显示实拍验证：三栏实际比例 **预览 57.0% / 中部 27.0% / 底部 16.1%**

---

## 🚀 快速开始

```bat
:: 1. 解压 pdf_merger_v0.8.3.zip
:: 2. 进入解压目录，双击 build.bat 或命令行：
build.bat

:: 3. 构建完成后运行：
PDFMerger_v0.8.3.exe
```

---

## 📝 本次发布对比基线（origin/main）新增文件

```
LICENSE                       ← 原已有
.gitignore                    ← 新增（zip / pycache / 用户配置）
requirements.txt              ← 新增（根目录直接 pip install）
build.bat                     ← 新增（根目录一键构建，exe 输出到同目录）
version_info.txt              ← 新增（exe 右键属性版本信息 v0.8.3）
pdf_merger/
  ├─ app.py                   ← UI 主程序
  ├─ pdf_engine.py            ← 合并引擎（PyMuPDF 矢量 + Pillow 拼图）
  ├─ worker.py / bus.py       ← 后台线程 + 主线程消息队列
  ├─ config.py                ← 配置数据类
  ├─ persistence.py           ← JSON 持久化（设置 + 文件列表 + 版本迁移）
  ├─ main.py                  ← 入口（可选 DnD 拖拽）
  ├─ build.spec               ← PyInstaller 配置（无控制台 / 单文件 / 版本号 exe 名）
  ├─ bump_version.py          ← 版本号一键升级
  ├─ version.py               ← 主版本号（0.8.3）
  ├─ smoke_ui.py / smoke_preview.py / test_engine.py
  ├─ make_icon.py / app.ico   ← 应用图标
  └─ app_preview.png          ← 运行实拍预览图
```

---

_Enjoy your printing-ready PDFs!_ 🖨️
